import React from 'react';

import { Button } from '@lib/ui-components';

const App: React.FC = () => (
  <div className="App">
    <header className="App-header">
      <Button>Test</Button>
    </header>
  </div>
);

export default App;
